App\DTO\ImageDTO:
    properties:
        file:
            - Image:
                maxSize: 500k
                maxSizeMessage: Image too big (500ko max)
                uploadIniSizeErrorMessage: Image too big (500ko max)
                allowSquare: false
                allowSquareMessage: Please don't use square images
                mimeTypes:
                    - 'image/jpg'
                    - 'image/jpeg'
                    - 'image/png'
                mimeTypesMessage: Only jpg, jpeg and png allowed
                groups:
                    - trickDTO