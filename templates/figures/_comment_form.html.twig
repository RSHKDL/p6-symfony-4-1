{{ form_start(form, {method: 'POST', action: path('comment_new', {'figureSlug': figure.slug})}) }}

{{ form_errors(form) }}

<div class="form-group">
    {# Render any errors for the "content" field (e.g. when a class property constraint failed) #}
    {{ form_errors(form.content) }}

    {{ form_widget(form.content, {attr: {rows: 5}}) }}
    {{ form_help(form.content) }}
</div>

<div class="form-group">
    <button class="btn btn-primary pull-right" type="submit">
        <i class="fa fa-paper-plane" aria-hidden="true"></i> Post
    </button>
</div>

{{ form_end(form) }}