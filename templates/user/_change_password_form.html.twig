{% form_theme form 'bootstrap_4_layout.html.twig' %}
{{ form_start(form, {
        method: 'POST',
        action: path('user_profile_change_password', {'id': user.id})
    })
}}
{{ form_errors(form) }}
{{ form_widget(form) }}
{{ form_help(form) }}
<button class="btn btn-primary" type="submit">
    <span class="fas fa-edit" aria-hidden="true"></span> Change password
</button>
{{ form_end(form) }}